{
  "name": "@himitsu/github-action",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "GitHub Action that loads secrets from a Himitsu environment.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node --test dist-test/tests/unit.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
